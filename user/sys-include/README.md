# sys-include

Please put header files which is **unrelated to musl libc** here.

## chcore-internal

This directory includes some header files which will used by user-space system servers. Please put header files used by system servers like `fs_defs.h`, `lwip_defs.h`, `sd_defs.h` here. 
