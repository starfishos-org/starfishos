if(CHCORE_SAMPLE_APP)
    add_compile_options(-fno-omit-frame-pointer)
    add_subdirectory(apps)
    add_subdirectory(utils)
    add_subdirectory(tests)
    chcore_copy_all_targets_to_ramdisk()
endif()
