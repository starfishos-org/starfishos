#!/usr/bin/env bash
# Stub: process migration time breakdown (paper Fig process-migration).
# See README.md for the intended workflow.
set -euo pipefail

AE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "[AE][stub] $AE_DIR/run.sh is not implemented yet."
echo "[AE][stub] Target figures: process-migration-data-large/small.eps"
echo "[AE][stub] Reuse: dsm-scripts/tests/cfork_*.exp, ckpt_*.exp; plot from p3os-paper/eval/process_migration.py"
echo "[AE][stub] Note: SSI-SLS/TreeSLS may be gated off in this tree — verify before implementing."
exit 2
