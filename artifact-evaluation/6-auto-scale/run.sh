#!/usr/bin/env bash
# Stub: auto-scaling Matrix / DBx1000 / GeminiGraph (paper Fig auto-scale).
# See README.md for the intended workflow.
set -euo pipefail

AE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "[AE][stub] $AE_DIR/run.sh is not implemented yet."
echo "[AE][stub] Target figures: auto-scale-matrix.eps, db1000.eps, gemini-chcore.eps"
echo "[AE][stub] Reuse: dsm-scripts/tests/{db1000,gemini}.exp, phoenix/; plot from p3os-paper/eval/"
exit 2
