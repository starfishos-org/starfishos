#!/usr/bin/expect  

set timeout 60
set script_path [lindex $argv 0]

if { $script_path == "" } {
    set script_path "run_pca.sh"
}

# start
spawn ./build/simulate.sh
expect "Welcome to ChCore shell!"
# run anything you like here
# interact
send -- "source $script_path\n"
expect "done"
exit
