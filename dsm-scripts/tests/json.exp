#!/usr/bin/expect  

set timeout 120
set file_path [lindex $argv 0]

if { $file_path == "" } {
    set file_path "english.json"
}

# start
spawn ./build/simulate.sh
expect "Welcome to ChCore shell!"
# run anything you like here
# interact
send -- "json_test.bin $file_path \n"
expect "done"
exit
